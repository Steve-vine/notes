---
id: 01KYA2TP0VKB8P003T6PY57677
created: 2026-07-24T12:50:27.483596Z
updated: 2026-07-24T14:48:55.43969Z
type: task
title: 'AI spend: By Provider daily breakdown (Last 30 days | Today | previous 14 days)'
project: 01KX671DATY39VW6GWK3M2T3DN
number: 249
sprint: sthz8ne
comments:
- id: 01KYA718PV2KKKWV3PA01SS11T
  author: Steve Vine
  at: 2026-07-24T14:03:57.531679Z
  text: |-
    Done — PR #232 (feature/ise-249-provider-daily-breakdown → main), stacked on ISE-247 (#231).

    - Added generic day-bucketed aggregation to ai_config_api: `_daily_matrix` (GROUP BY dimension, date_trunc('day', started_at, 'UTC')) + `_breakdown` assembler producing a dense dimension × day table — today + 14 previous days, plus the 30-day window total, with a Total row. Built once and generically so ISE-250 (By Task) and ISE-251 (By Integration) reuse it.
    - AISpendRead.by_provider_daily carries the provider breakdown; the per-provider key-configured / ceiling-reached indicators stay on `providers` (which SpendLimitsCard also reads), and the daily table joins to them by provider name.
    - New reusable DailySpendTable component (horizontally scrollable, Total footer). AISpendCard's provider section now renders it as "By provider".

    UTC day bucketing via three-arg date_trunc so the day columns and the daily-ceiling accounting agree regardless of DB session timezone. A provider with no runs still appears as an all-zero row (the property ISE-250's reconciliation leans on).

    Tests: unit tests for `_day_columns`/`_breakdown` + an end-to-end endpoint test seeding runs across days. API types regenerated. Backend green; frontend build + prettier + eslint clean.
assignee: steve
priority: medium
task_status: review
---
The "Today, by provider" section of `AISpendCard.tsx` shows a single today-only figure per provider. Rework it into a daily-breakdown table:

```
             Last 30 Days | Today | Thu 23 | Wed 22 | Tue 21 | ... (14 previous days)
Anthropic            9.21    0.11     0.23     1.23     0.32
OpenAI               0.44    1.02     0.21     0.11     0.01
Total                x.xx    x.xx     x.xx     x.xx     x.xx
```

Backend: `GET /api/v1/ai-config/spend` (`ai_config_api.py:137-195`) has no day dimension — add day-bucketed aggregation over `agent_run.cost_usd` (`GROUP BY provider, date_trunc('day', started_at)` over the last 30 days) to the response model. Keep the existing key-configured / ceiling-reached indicators per provider row.

UI: horizontally scrollable table, Total row at the bottom. This endpoint change is shared by the By Task (ISE-250) and By Integration (ISE-251) tasks — build the day-bucketed shape once, generically (dimension × day).