---
id: 01KY725SRM4WT2N7P4V5Z6X8W7
created: 2026-07-23T08:41:19.892648Z
updated: 2026-07-23T09:08:58.555016Z
type: task
title: Right-hand panel
label: brief
task_status: done
comments:
- id: 01KY72612EZA6N25SHHYD11WT3
  author: Steve Vine
  at: 2026-07-23T08:41:27.3745Z
  text: |-
    Steve Vine · 2026-07-09:

    Done — PR [#245](https://github.com/Steve-vine/notuvia/pull/245).

    **What was done:** the agreed plan in full — the canvas's `selectedId` surfaced to Main via a bindable prop, `selectedNoteId` in workspace mode now `overlay ?? canvas selection`, and the Sprints section for selected Projects by generalising the Kanban wiring (`loadProjects` in workspace mode; `boardProjectId` workspace case gated on project-ness so the sprints/tasks fetch never runs for memos/tasks; `ProjectSection` rendered in the panel's extra snippet). Pure wiring — 2 files, no new components, no backend change.

    **Decisions made on the fly:**
    - Sprint picks from the workspace panel take the DEV-837 jump path (`openSprintBoard`) — there's no board to filter in place, so it switches to that project's Kanban board with the filter applied; the mode change also clears the canvas selection via the existing reset effect.
    - The workspace Sprints section shares the Kanban section's collapse pref (`kanban.projectCollapsed`) — same "Project section" concept, avoids another tab-document field.

    **Problems encountered:** none.

    **Verification:** `vitest run` 152/152, `npm run check` clean. Manual pass: click icons → Properties/Taxonomies follow each click (title edits update the icon label live); empty-canvas click → placeholder; Project icon → Sprints with open counts, sprint click jumps to the filtered board; the overlay takes precedence while open and hands back on close.
assignee: steve
priority: medium
project: 01KY6W9951TW0904DT0GGJVGE7
number: 262
---
On the right hand panel in Workspace mode, show the Properties and Taxonomies sections, updating as a note icon is clicked on the canvas. When a Project is selected, also show the Sprints section.

**Agreed plan (2026-07-09):** pure wiring — the panel already renders in workspace mode but only tracks the overlay; canvas selection never leaves `WorkspaceView`, and the Sprints `ProjectSection` is Kanban-gated.

- [ ] `WorkspaceView.svelte`: promote the internal `selectedId` to a `$bindable()` prop (KanbanView pattern); all existing select/clear writes propagate through the binding.
- [ ] `Main.svelte`: new `canvasSelectedId` state bound to the view, cleared in the existing tab/mode-change reset effect; `selectedNoteId` in workspace mode becomes `boardOverlayNoteId ?? canvasSelectedId` (overlay wins, like Kanban). Properties + Taxonomies then follow icon clicks for free via PropertiesPanel's shared DocHandle (ADR 0010).
- [ ] Sprints: generalise the Kanban wiring — `loadProjects` effect also runs in workspace mode; `boardProjectId` gains a workspace case gated on project-ness (`projects.some`), so the existing sprints/tasks fetch, `boardProject`, and open-count deriveds work unchanged; render `ProjectSection` in the panel's extra snippet for workspace mode with `sprintFilter={null}` — picking a sprint takes the DEV-837 jump path to that project's filtered Kanban board.

No backend change; no new components.

---

Linear DEV-915 · Workspaces · created 2026-07-09 · done 2026-07-09