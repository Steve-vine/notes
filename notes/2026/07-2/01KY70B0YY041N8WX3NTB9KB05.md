---
id: 01KY70B0YY041N8WX3NTB9KB05
created: 2026-07-23T08:09:13.950812Z
updated: 2026-07-23T11:03:33.862328Z
type: task
title: 'Editable right panel: Properties + Taxonomies sections, meta header removed'
task_status: done
comments:
- id: 01KY70BANSATM35CAA76RH5K6B
  author: Steve Vine
  at: 2026-07-23T08:09:23.897543Z
  text: |-
    Steve Vine · 2026-07-02:

    Done building — PR: https://github.com/Steve-vine/notuvia/pull/130

    **What was done**
    - `NoteProperties.svelte` (new, 450 lines): the meta header's pickers restacked vertically on the shared `DocHandle` — Properties section (Type/Project/Milestone pills, identifier, Status/Priority via `MetaTaxonomyPicker`, the new Due date input, read-only ID/Created/Updated, project milestones manager) + Taxonomies section (`TaxonomyEditor`, untouched and still shared with the capture window).
    - `PropertiesPanel.svelte`: `loadNote`+`noteRev` polling replaced with `acquire`/`release` of the shared buffer (NotePane's `held`/`currentId` pattern) — panel edits autosave, respect the conflict guard, and sync live with any open pane; head shows the live note title + collapse chevron; section collapse persisted globally as `notuvia:rightSections`.
    - `NotePane.svelte`: 1289 → 882 lines. Meta header fully removed; title flows straight into the body. The buffer error line + changed-on-disk guard relocated to a slim strip above the content.
    - `NoteDoc` gains read-only `created` (the panel displays it; only `loadNote` carried it before).

    **Decisions made on the fly**
    - The error/conflict notices stay with the **pane**, not the panel — they belong to the buffer being edited in that pane, and the panel may be showing a different note (e.g. a kanban project).
    - The taxonomyRev→`refreshDefs` effect moved from NotePane to PropertiesPanel — the pane no longer renders any defs-driven UI; the panel does.
    - Panel section collapse is a **global** preference (`notuvia:rightSections`), not per-tab — the panel itself isn't per-tab under ADR 0021.
    - Due date edits commit on the date input's `change` (not per keystroke) to avoid autosaving half-typed dates.

    **Problems encountered**
    - None. svelte-check 0/0, vitest 109/109, gate green, app boots cleanly. Review pass: panel↔pane live sync on the same note; due date round-trip to frontmatter; identifier + milestones editing from the panel; conflict banner on external edit while dirty; the panel against a kanban-selected project.
assignee: steve
imported_from: linear
priority: medium
project: 01KY6W9951TW0904DT0GGJVGE7
number: 145
sprint: sa8cznq
---
R8 of the Revamp UI milestone (parent DEV-754). Note metadata moves from the in-note header to an editable right panel with one write path.

* New `NoteProperties.svelte` working on a shared `DocHandle`: **Properties** section (Type/Project/Milestone pickers, identifier input + milestones manager, Status/Priority via existing `MetaTaxonomyPicker`, new Due date input, read-only ID/Created/Updated) — lifted from NotePane's meta header — and **Taxonomies** section (existing `TaxonomyEditor`, which stays generic for the capture window).
* `PropertiesPanel.svelte` switches from its own `loadNote` to `acquire`/`release` of the shared buffer (mirrors NotePane's held/currentId pattern) → autosave, conflict handling, and live sync with open panes for free.
* `NotePane.svelte` loses the whole meta header; the doc-error line and changed-on-disk banner relocate to a slim strip above the content.
* Selection semantics in Main: browse = active pane's note; kanban = selected card ?? selected project (card selection itself lands in R10).

Depends on: DEV-763 (R3, CollapsibleSection), DEV-767 (R7, due field).

Checklist:

- [ ] NoteProperties on DocHandle (Properties + Taxonomies)
- [ ] Panel edits sync live with an open pane on the same note
- [ ] Meta header removed; error/conflict strip preserved
- [ ] Due date editable

---

Linear DEV-768 · Revamp UI · created 2026-07-02 · done 2026-07-02