---
id: 01KYC62VA9753RHR4R5659JF11
created: 2026-07-25T08:25:49.641067Z
updated: 2026-08-05T13:39:22.301684Z
type: task
title: Alert-level webhook events raise signals
project: 01KX671DATY39VW6GWK3M2T3DN
number: 279
sprint: s6pc5xk
blocked_by:
- 01KYC4YWCCZM0GHY37VMHWDTER
comments:
- id: 01KYCDYF9B0PD1PV16MJMEAN8V
  author: Steve Vine
  at: 2026-07-25T10:43:14.859133Z
  text: |-
    Done. Alert-level webhook events now raise ordinary Alert signals that flow through the whole incident spine unchanged.

    Linkage decision (ADR 0048, migration 0052): a Finding requires system_id and a webhook source isn't a System, so I evaluated (a) nullable system_id + finding.webhook_source_id vs (b) a synthetic System per source. Chose (b) because it reuses the entire promotion/correlation/severity/alerts/incident machinery verbatim — the riskiest code — with zero changes. Each alerting source is backed by a synthetic System(connector_type='webhook'), created LAZILY on first alert (webhook_source.system_id). A registered, hidden, no-op WebhookConnector (new `hidden` flag on the connector base, filtered from the add-integration picker) makes get_connector resolve the type so no capability code special-cases it; the system has no credential, sync_interval_seconds=None and obs disabled, so is_due/is_obs_due both skip it — it's never synced, findings only arrive by push.

    - webhook_signals.raise_signal_for_event: level→severity map (alert→high; per-source tuning comes free via the ADR 0026 override layer since each source has its own system); dedup on (system, source_key=alert_key, which defaults to event_type); re-fire → Recurring (not duplicate); entity-hint resolution (exact integration-key match then name, never invents an entity); then promote_findings runs → incident opens above threshold / correlates. Wired into the ingest endpoint (only for level==alert or status==recovered; warn/info stay context-only).
    - Recovery (push source can't be polled): explicit status:recovered event with the same alert_key, OR the per-source alert_ttl_seconds TTL sweep (recover_stale_webhook_alerts, new 60s Beat task) — both mark the finding recovered and re-promote to settle the incident.
    - webhook_event.finding_id links an event to the signal it raised; exposed in the events API and linked from the Events detail modal ("Raised an alert signal — view on Alerts"). Signals surface on the existing Alerts screen with the source name as origin. No new screen.

    Acceptance tested end-to-end via the real ingest endpoint: POST alert → Finding(alert, high) on the synthetic system + Incident opens; re-fire → one finding (deduped); recovered event → Recovered; TTL expiry → Recovered; entity hint resolves (matched→entity_id, unmatched→None); warn/info raise nothing and mint no system.

    ADR 0048 written. 6 signal tests; no regressions across connectors_api/connectors/promotion/sync/migrations/worker-registration (58) + 39 webhook tests total; mypy (304) + ruff clean; frontend build + eslint + prettier + vitest green. Committed to feature/ise-279-alert-webhook-signals (stacked on ise-278).
assignee: steve
priority: medium
task_status: done
---
Webhook events become a cheap detection path for systems with no supported integration: a sender that POSTs `level: alert` is a source with its own detection layer, so ISE forwards its verdict as an Alert signal — the Canon deferral principle, DataDog-monitor behaviour without DataDog.

- **Level → ladder mapping**: per-source mapping of webhook levels onto the canonical severity ladder (default e.g. `alert→high, warn→medium, info→info`), like any connector's alert mapping. The severity-override layer and global auto-incident threshold apply unchanged — no new promotion machinery.
- **Signal creation**: an alert-level event creates/updates a `Finding(signal_type=alert)`. Identity/dedup on `source + alert_key` (defaulting to event type) so a re-fire attaches (Recurring) instead of duplicating; incident correlation rides the existing open rule.
- **Entity resolution**: the event's `entity` hint resolves against estate names/aliases; matched → signal carries `entity_id` and joins the graph, unmatched → signal without entity (never invent entities).
- **Recovery** (the push-source problem — ISE cannot poll the sender): an event with `status: recovered` and the same `alert_key` marks the signal Recovered; additionally a per-source **TTL fallback** (nullable column from ISE-274, editable in the ISE-275 Settings UI) auto-recovers alerts not re-fired within the window, via a Beat sweep.
- **UI**: these signals surface on the existing Alerts screen and incident timeline like any other alert, with the webhook source named as origin; the event modal (ISE-276) links to the raised signal. No new screen.

Acceptance: POST an alert-level webhook → Alert signal appears on the Alerts screen and (above threshold) opens an Incident; re-fire attaches; a recovered-status webhook or TTL expiry moves it to Recovered.