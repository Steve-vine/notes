---
id: 01KY7078MWKSY7Z453YAC32M83
created: 2026-07-23T08:07:10.748618Z
updated: 2026-07-23T11:04:50.702327Z
type: task
title: View-mode tabs in main panel + collapsible sidebar sections + Dashboard placeholder
comments:
- id: 01KY707HGBJF9PVNRNM7D43X6Q
  author: Steve Vine
  at: 2026-07-23T08:07:19.818764Z
  text: |-
    Steve Vine · 2026-07-02:

    Done building — PR: https://github.com/Steve-vine/notuvia/pull/125

    **What was done**
    - `ViewTabs.svelte`: Dashboard | Browse | Kanban underline tabs at the top of the main panel; sidebar tab strip removed, with the sidebar-collapse chevron rehomed to the footer actions.
    - `CollapsibleSection.svelte`: reusable lighter-header section — label left, caret right, bindable `collapsed`, optional `actions` snippet (ready for the R6 axis "+").
    - Browse mode = collapsible **Search** section + **Browse** section, both live at once; Kanban mode = **Projects** (`[All Projects]`) + **Tasks** (`[All Tasks]`, `[Ungrouped]`, projects) sections with unchanged selector behaviour.
    - `Dashboard.svelte` placeholder view.
    - `Main.svelte` rewired to `ViewMode` (`dashboard | browse | kanban`), default **Browse**.

    **Decisions made on the fly**
    - `ViewMode` type is exported from `ViewTabs.svelte` for now; it moves into `workspace.svelte.ts` with DEV-764 rather than earning a one-type module today.
    - Merge consequences resolved as: Browse refreshes rerun search *and* reload values; Browse export = search results ∪ expanded values; opening a board card lands on Browse (`lastNoteViewTab` deleted — R10's overlay supersedes the whole pattern).
    - Kept `[Ungrouped]` as the label (the mockup shows "[Stray Tasks]") — a rename is cheap but is a naming decision worth making consciously, not smuggling into this brief. Flag it in review if you want it.
    - Section-collapse state is session-local (not persisted) by design — it becomes per-workspace-tab state in DEV-764.

    **Problems encountered**
    - Couldn't screenshot the running app for visual confirmation: `screencapture` is denied (terminal lacks macOS Screen Recording permission). The app launches cleanly under `tauri dev` with no errors; the visual pass needs your eyes in review — tabs at top, section collapse/expand, Dashboard placeholder, Kanban sections, and that search + browse still behave inside their sections.
assignee: steve
imported_from: linear
task_status: done
priority: medium
project: 01KY6W9951TW0904DT0GGJVGE7
number: 140
sprint: sa8cznq
---
R3 of the Revamp UI milestone (parent DEV-754). Moves the view-type tab bar out of the sidebar and restructures the sidebar into collapsible sections.

* New `ViewTabs.svelte`: "Dashboard | Browse | Kanban" underline tabs at the top of the **main** panel; the sidebar tab strip is removed.
* "Search" and "Browse" merge into the single **Browse** mode.
* New reusable `CollapsibleSection.svelte` (lighter header row with caret + optional actions).
* `Sidebar.svelte` becomes a section host: Browse mode → "Search" + "Browse" sections; Kanban mode → "Projects" + "Tasks" sections (current selector behaviour).
* New `Dashboard.svelte` placeholder center view (content is a later follow-up).

Depends on: DEV-762 (R2).

Checklist:

- [ ] ViewTabs in main panel, sidebar tab strip removed
- [ ] CollapsibleSection component + per-mode sidebar sections
- [ ] Dashboard placeholder renders
- [ ] Search + browse behaviour unchanged inside their sections

---

Linear DEV-763 · Revamp UI · created 2026-07-02 · done 2026-07-02