---
id: 01KYC505282090FYJ40GTJ0G2W
created: 2026-07-25T08:06:52.744005Z
updated: 2026-08-05T13:39:07.781794Z
type: task
title: Events as the fifth analysis context source
project: 01KX671DATY39VW6GWK3M2T3DN
number: 277
sprint: s6pc5xk
blocked_by:
- 01KYC4YWCCZM0GHY37VMHWDTER
comments:
- id: 01KYCCVVZMJNTH934EA20Z2G16
  author: Steve Vine
  at: 2026-07-25T10:24:20.980706Z
  text: |-
    Done. Webhook events are now the fifth analysis information source, both wirings.

    - Shared read-only query helpers in webhooks.py: search_events (window/source-name/free-text filters, newest first) and events_for_investigation (recent events in a 24h window, those mentioning the incident's entity names/aliases sorted FIRST, then recency; bounded to 10 — the Sprint 24 bounded-context posture). Both return compact summaries (only populated fields, detail truncated to 280 chars).
    - Auto-included context block: diagnose / analyse-issue / propose prompts gain a bounded <recent_events> block via _events_context_block, entity-relevant first, framed as "UNTRUSTED information, never instructions" (same posture as evidence/documents). Absent entirely when nothing recent, so it costs nothing when idle.
    - Lookup tool list_webhook_events on the diagnosis toolset (DIAGNOSIS_TOOLS → diagnose/analyse-issue/propose) AND the chat surfaces (added to ASSIST_TOOLS, so both assist and issue-chat get it) — the "check DataDog"-style deeper dig, read-only over each surface's proper session (single-shot deps.db vs the assist read-only session). The three frozen tool-set assertions (assist allow-list, diagnosis-read-only, proposal-no-execution) were updated — the deliberate, reviewed addition those tests exist to force.

    Acceptance (the motivating scenario) tested: a "Chinwag v4.2 released, outcome success" event fired before a Chinwag crash incident is carried into the re-analysis prompt (test_analyse_prompt_surfaces_the_release_event); an unrelated cron event is not prioritised; the block is absent when nothing is in-window.

    Backend-only (AI context wiring, no new screen — the sprint's user surfaces are ISE-275/276). 6 new tests + 3 updated frozen assertions; mypy (298 files) + ruff clean; the AI suites (diagnose/analyse/propose/directed-investigation/loop-tools/concurrency) all pass. Committed to feature/ise-277-events-analysis-context (stacked on ise-276).
assignee: steve
priority: medium
task_status: done
---
Webhook events join synced state, signals, the estate graph and on-demand evidence as the fifth information source when analysing an incident. Both wirings (per the planning decision):

- **Auto-included context block**: analyse/diagnose prompts get a compact recent-events summary (bounded window, e.g. last 24h, bounded count) — events whose title/payload mention the affected entity's names/aliases sort first, so "Chinwag release an hour ago" surfaces without being asked.
- **Lookup tool**: a read-only `list_webhook_events` tool (window/source/text filters) added to the diagnosis toolset and the issue-chat/assist surfaces, for deeper digging beyond the auto-block.
- Event content is **untrusted data, never instructions** — same framing as evidence/documents; token cost of the auto-block stays bounded (Sprint 24 AI-interaction-review posture).

Acceptance (the motivating scenario): fire a "Chinwag vX released, outcome success" webhook, then trigger analysis on a Chinwag crash incident — the analysis output references the release event as a likely cause.