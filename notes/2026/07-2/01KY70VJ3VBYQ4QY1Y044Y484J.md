---
id: 01KY70VJ3VBYQ4QY1Y044Y484J
created: 2026-07-23T08:18:15.803525Z
updated: 2026-07-23T12:25:00.310359Z
type: task
title: Assignee taxonomy
label: null
task_status: done
assignee: steve
priority: medium
project: 01KY6W9951TW0904DT0GGJVGE7
number: 172
comments:
- id: 01KY70VQH0M4YMDRZEGAV6HNAR
  author: Steve Vine
  at: 2026-07-23T08:18:21.344456Z
  text: |-
    Steve Vine · 2026-07-04:

    Done as agreed:

    - Assignee values have an optional **initials** field, edited per value in Settings (short input, max 4 chars). The 'Done' checkbox was already gone from Assignee via DEV-813's status-only gating.
    - The card's top-right avatar is now a **pill**: explicit initials when set, falling back to the label's first letter. One character still renders as the original circle; longer initials widen the pill.
    - **Project cards** carry the assignee pill too: Assignee's scope is now Task+Project (via ADR 0023 frame normalisation), so the picker also appears on project notes automatically.

    **Decisions on the fly:** initials input caps at 4 characters and trims whitespace; empty clears back to the fallback. Unset initials deliberately return null so the frontend owns the fallback (one place to change the rule).

    **Problems encountered:** none. Full gate green (165 Rust + 110 frontend tests).

    To verify: Settings → Assignee → set initials on a value, assign it to a task and a project, check both cards show the pill top-right.

    PR: https://github.com/Steve-vine/notuvia/pull/155
sprint: ssy6aak
---
From the Assignee taxonomy, remove the 'Done' field and add an initials field.  Show this field in the top right hand corner of cards when selected in a pill.

Add this to project cards as well.

## Agreed work (planned 2026-07-04)

* **Initials field**: `TaxonomyValue` gains an optional `initials` (Rust + TS). The Settings editor shows a short per-value "initials" input for the Assignee taxonomy. (The 'Done' checkbox was already removed from Assignee by DEV-813 — it's status-taxonomies-only now.)
* **Assignee applies to Projects**: seed scope becomes Task+Project, delivered to existing vaults by ADR 0023's frame normalisation (landed in DEV-813). The assignee picker appears on project notes automatically.
* **Cards**: `BoardCard` gains `assignee_initials`; the task board resolves it alongside the label, and the project board now populates assignee fields (the query column landed in DEV-813). The card's top-right avatar becomes a pill rendering the explicit initials, falling back to the first letter of the label when unset — on task and project cards alike.
* Tests: initials round-trip; task card carries initials when set / falls back when not; project card assignee fields populated.

---

Linear DEV-814 · Kanban board Improvements · created 2026-07-04 · done 2026-07-04