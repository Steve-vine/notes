---
id: 01KY70G8BRH8W291SMW3J3V6C7
created: 2026-07-23T08:12:05.368918Z
updated: 2026-07-23T11:00:30.158669Z
type: task
title: Borderless split panes, dimmed inactive pane + icon pass
task_status: done
comments:
- id: 01KY70GGRSV2EENS6F9SKRN672
  author: Steve Vine
  at: 2026-07-23T08:12:13.977215Z
  text: |-
    Steve Vine · 2026-07-02:

    Done building — PR: https://github.com/Steve-vine/notuvia/pull/133 (stacked on PR #132 — see below)

    **What was done**
    - `Pane.svelte`: dividers are transparent 8px gaps — canvas shows through, the gap is the drag zone; hit area, pointer capture, double-click 50/50 and keyboard resize all kept; no hover highlight, faint accent only while dragging/focused.
    - `NotePane.svelte`: soft grey panels via a new `--pane-bg` token (light + dark), radius, no border; active-pane accent border removed — inactive panes dim their statusbar/content wrappers to 0.7 (wrappers, not the pane, so popovers keep contrast); statusbar bottom border gone.
    - Browse mode gains 0.5rem padding around the panels (the board pads itself).
    - Icon pass: statusbar ⬌/⬍/✕ text glyphs → new `split-row`/`split-col` SVG frames + `x`, with icon-button styling; `CollapsibleSection`'s text caret → the chevron icon (down open, left collapsed) across all sections.

    **Decisions made on the fly**
    - Stacked on `brief-770-card-select-overlay` rather than main — both briefs touch NotePane and sequencing would have idled this one behind review. Per ways-of-working: **merge #132 first**; I'll then rebase this onto main and retarget the PR before the parent branch is deleted.
    - Keyboard-focus keeps a faint divider tint (unlike hover) — a keyboard user needs to see which divider is armed.
    - The icon pass stayed scoped to the statusbar buttons + section carets; the browse tree's tiny text carets are consistent within the tree and can follow later if wanted.

    **Problems encountered**
    - None. svelte-check 0/0, vitest 109/109, gate green. Visual pass in both themes: gap-drag feel, dimmed inactive panes (popovers full-contrast), single-pane + overlay chrome, new icons.
imported_from: null
assignee: steve
priority: medium
project: 01KY6W9951TW0904DT0GGJVGE7
number: 148
sprint: sa8cznq
label: null
---
R11 of the Revamp UI milestone (parent DEV-754). Final polish brief.

* `Pane.svelte`: dividers become transparent gaps (keep the enlarged `::before` hit area + pointer capture; at most a faint accent while dragging, nothing on hover).
* `NotePane.svelte`: panes become soft grey panels (`--pane-bg` token in `theme.css`, radius, no border); the active-pane accent border goes; inactive panes dim inner content (`opacity: .7` on content/statusbar wrappers, not the pane, so popovers aren't dimmed); statusbar loses its bottom border.
* Icon pass: cleaner/simpler `Icon.svelte` glyphs across the shell per the mockups.

Depends on: DEV-768 (R8 — style the post-meta-header pane, avoid churn). Land last.

Checklist:

- [ ] Gap dividers, no hover highlight
- [ ] Grey panel chrome, dimmed inactive panes (light + dark themes)
- [ ] Icon refresh

---

Linear DEV-771 · Revamp UI · created 2026-07-02 · done 2026-07-02