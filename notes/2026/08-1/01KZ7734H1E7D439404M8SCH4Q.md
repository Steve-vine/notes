---
id: 01KZ7734H1E7D439404M8SCH4Q
created: 2026-08-04T20:22:57.313804Z
updated: 2026-08-06T08:15:03.400761Z
type: task
title: One filter dropdown for the whole app — CheckboxFilter everywhere, opening downward, multi-select, clearable, alphabetical
project: 01KX671DATY39VW6GWK3M2T3DN
number: 544
sprint: skxht3g
comments:
- id: 01KZ8PKBKFE2DEY0S6HTCGJZXA
  author: Steve Vine
  at: 2026-08-05T10:13:11.919083Z
  text: |-
    Done — PR #469 (feature/ise-544-one-filter-dropdown). CI running; local gates all green (frontend 598 tests + lint/prettier/build, backend 2291 tests + ruff/mypy, API types regenerated).

    **A. Two fixes in CheckboxFilter itself**, so nobody can opt out by accident:
    - Pinned downward — `position="bottom-start"` + `middlewares={{ flip: false }}`. Floating-UI flips to whichever side has room, which is why Incidents opened upward and the same control opened downward elsewhere. The options list already scrolls internally (`mah={280}`), so a short viewport never needed the flip.
    - Alphabetical by default, sorted inside the component so no caller can produce an unsorted filter. `order="ranked"` is the explicit opt-out — used for severity AND for the lifecycle statuses (Signal Status, Incident Status, Alert Status), which alphabetising scatters just as badly. Severity constants were also flipped to worst-first while I was there.

    **B. Adoption** — every filter-section Select is now a CheckboxFilter: Estate (type, integration, operated-by), Events (source, type, outcome, level), Platform Log (level, component), Audit (action, entity type), Agent runs (integration, task type, status), Proposals (kind, source), Signals (integration, severity), Applications (environment), Tag cloud (integration — that one was a MultiSelect, the very widget CheckboxFilter exists to replace per ISE-175).

    I went for **real multi-select rather than look-alike single-select**: seven endpoints grew repeatable params and OR their values (entities, findings, events, audit, agent-runs, proposals, platform-log). Two ticked types means "hosts AND workloads", which is the only reading that makes ticking a second box useful. Two whitelists moved from `Query(pattern=…)` into the item TYPE — pydantic applies a pattern to the LIST, not its items — with tests pinning the Literals against the model constants. ENTITY_TYPES is too long and too mobile to duplicate as a Literal, so entity type is checked explicitly against the constant and still 422s.

    Persisted filter keys bumped (`:v3`/`:v4`) rather than migrated — a stored scalar would arrive where an array is expected.

    **Three things I left out, and why** — flagging in case you want any of them reversed:
    1. **Time windows** (Platform Log, Events, Tag cloud) and **Sort by** — single-valued by nature, your own carve-out.
    2. **The Proposals queue's Status** — this is the one judgement call. It selects a VIEW, not a value: "Still true?" is not a status at all but a different question asked of confirmed rows (it ignores `status` server-side), so ticking it alongside "Rejected" would ask for something with no meaning. Left as a Select with a comment explaining it. Say the word and I'll make it multi-select over the four real statuses and move "Still true?" elsewhere.
    3. **Form Selects** (settings, playbook authoring, rule builders) — forms, not filters, per the ticket.

    Both carve-outs are now written into `docs/briefs/design-system.md` next to the rule itself, so the next filter added follows it without asking.

    Guards verified failing with the component reverted: alphabetical ordering, and the downward pin — asserted on the props via a Combobox spy, because jsdom does no layout so it can never exercise a flip, and `data-position` is Mantine's default either way (the ISE-515 lesson). Backend adds OR-semantics guards for entity type and for the two operator values being exhaustive.

    To smoke on staging: open the filter panel on Issues, Signals, Estate, Events, Audit, Platform Log, Agent runs, Applications, Proposals, Tag cloud — every dropdown should open downward, tick multiple values, clear from its own X, and list alphabetically, except Severity and the status filters which keep their meaningful order.
assignee: steve
priority: medium
task_status: done
---
From functional testing 2026-08-04 (Steve): filter dropdowns across the app behave and look differently page by page. Standardise them — every filter dropdown should:

1. **Open downward** (the Incidents/Issues page one opens upward today)
2. **Support multi-select**
3. **Be clearable per-dropdown** (one control clears that filter's selections)
4. **List entries alphabetically**
5. **Look identical everywhere**

## The good news — the target component already exists

`CheckboxFilter` (`components/CheckboxFilter.tsx`, ISE-175/ISE-477-490 FilterPanel work) already does most of this: fixed-width one-line trigger that never grows with selections ("3 selected"), tick-list multi-select, and an `Input.ClearButton` per dropdown. The task is **adoption + two behaviour fixes**, not a new component.

Current adoption is split (grep survey 2026-08-04):

| Widget | Where |
|---|---|
| `CheckboxFilter` (multi, clearable) | IssuesPage (5×), SignalsPage |
| Plain single-`Select` | EstatePage (4×), EventsPage (3×), PlatformLogPage (3×), AuditPage (2×), AgentRunsPage, ApplicationsPage, DashboardsPage, TagCloudPage, SettingsPage, PlaybookDeskSection |

## Scope

**A. Fix the two behaviour gaps in `CheckboxFilter` itself** (everyone inherits):
- **Direction**: pin the dropdown downward — Issues (the "Incidents" screen) opens upward today. Mantine Combobox flips by available space; the panel sits near the top of the page, so upward means it was let float. Pin `position: 'bottom-start'` with `middlewares={{ flip: false }}` (or equivalent) so direction is constant everywhere. If pinning can clip on short viewports, prefer constant-downward + internal scroll — consistency was the ask.
- **Sort**: options render in caller order today. Sort by label (`localeCompare`) inside the component, so no caller can produce an unsorted filter. Exception to decide in plan mode: **severity** filters — critical>high>medium is arguably the right order for that one; if kept, make it an explicit opt-out prop (`order="ranked"`) rather than caller-side sorting, so the default stays alphabetical.

**B. Migrate every filter-section `Select` to `CheckboxFilter`** — the full list above. Notes:
- Each migration turns a single-value filter param into a list; most backend endpoints take one value, so either the UI ANDs are re-queried per value, the param becomes repeatable (`type=a&type=b`), or the endpoint grows list support — **check per page**; several (Estate `type`, Events `event_type`, Platform Log `level`) may be single-valued in the API today. Where the API is single-valued and extending is disproportionate, migrating to the same *visual* component in single-select mode is acceptable — look-alike was the ask; note it in the PR.
- **Sort/order controls are NOT filters** — a "Sort by" Select (Estate list, Tag cloud) keeps its ranked order and single-select; leave those out of scope, but restyle if trivially cheap.
- Settings-page form Selects (model pickers etc.) are forms, not filter sections — out of scope.

**C. Regression guards**: a shared test for CheckboxFilter asserting downward opening (jsdom can assert the position prop/data-attribute — the ISE-515 lesson: assert the property that carries the intent), alphabetical order, multi-select toggling and clear-per-dropdown. Per-page tests keep passing with the new widget (the `getByLabelText`-matches-column-headers gotcha from the ISE-477 batch applies).

## Definition of done

Every filter section in the app presents the same dropdown: opens downward, multi-select with count label, per-dropdown clear, alphabetical entries (severity optionally ranked by explicit prop). Verified by eye on: Issues, Signals, Estate, Events, Audit, Platform Log, Agent Runs, Applications, Dashboards, Tag Cloud.
