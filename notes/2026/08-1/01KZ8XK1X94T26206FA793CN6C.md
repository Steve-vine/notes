---
id: 01KZ8XK1X94T26206FA793CN6C
created: 2026-08-05T12:15:22.02599Z
updated: 2026-08-05T19:29:52.44401Z
type: task
title: 'Datadog connector: pull Synthetics test definitions and failed-run results for triage'
project: 01KX671DATY39VW6GWK3M2T3DN
number: 559
sprint: scb3vol
comments:
- id: 01KZ9BM7J3HH680AM27GE3F57V
  author: Steve Vine
  at: 2026-08-05T16:20:40.643437Z
  text: |-
    Built on feature/ise-559-datadog-synthetics, PR #480 (targeting main), merged to staging.

    Went the on-demand evidence route (no eager sync, no idle spend), per the task's either/or:

    - New synthetics_test evidence query: the test definition (name, type/subtype, status, request method+URL, assertions, locations, tick_every schedule, message) plus recent runs newest-first — and for up to three failing runs the FULL result is pulled for the exact failed assertion (code + message), HTTP status code, response time and probe location. Browser tests use the browser-result endpoints. Takes public_id or monitor_id; the TEST carries the monitor_id (the only place DataDog states the pairing), so a monitor id resolves via one list_tests call.
    - Linkage at detect time: a firing synthetics-alert monitor's finding now carries details.synthetics_public_id + synthetics_url, resolved lazily (only when a synthetics alert is in the batch) and contained — a key without synthetics_read logs debug and moves on, never killing the alert sync.
    - UI: Signal Detail Modal shows "Synthetics test: <public_id>" in the details grid and gains an "Open synthetics test" click-through beside "Open in DataDog"; the evidence query description tells incident chat the finding's details carry the public_id.

    ACTION NEEDED at smoke-test time: synthetics_read added to the connector's declared read-only scopes — verify the live DataDog app key actually carries it (per the task's own note), then re-ask the incident chat what the IN-1210 monitor's test does.

    Tests: connector contract tests (fake apis bundle, no live DataDog) — catalogue, full payload by public_id (JSON-serializable), monitor_id resolution + refusals, detect linkage on synthetics alerts only, detect survives a 403 on the lookup. 53 passing; mypy/ruff/frontend gates clean.
assignee: steve
priority: medium
task_status: done
---
Found during Sprint 50 incident-management testing: asked the incident chat what synthetic test a failing Datadog Synthetics monitor performs and what the failure result is — it couldn't answer. Confirmed this is an ISE capability gap, not an API limit: the Datadog connector (`connectors/datadog.py`) has no synthetics support; only monitor alert metadata is synced.

## What the Datadog API offers — CONFIRMED against docs.datadoghq.com 2026-08-05

- `GET /api/v1/synthetics/tests/{public_id}` — full test definition: request/steps, assertions, locations, schedule (API-test and browser-test variants exist; browser via v2).
- `GET /api/v1/synthetics/tests/{public_id}/results` and `/results/{result_id}` — per-run results: pass/fail, the failed assertion, HTTP status code, response time, per probe location.
- Requires `synthetics_read` permission on the API/app key (verify the current key has it).

## Scope

- Sync (or fetch on demand as an evidence tool, like `query_metrics`/`search_logs`) the synthetics test behind a monitor, and its recent failing results, so incident chat / diagnosis can state what the test does and the exact assertion that failed.
- Link from a synthetics-monitor finding to its test via the monitor's synthetics test id/public_id.
- Surface in the UI on the incident where the alert's monitor is a synthetics check.