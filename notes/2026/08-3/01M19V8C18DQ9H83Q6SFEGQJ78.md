---
id: 01M19V8C18DQ9H83Q6SFEGQJ78
created: 2026-08-30T17:25:12.872327Z
updated: 2026-09-01T13:55:52.346467Z
type: task
title: Navigating away from any half-finished form asks first, not just stepping between controls
project: 01KXGC5PTGYHV30VM3E78G76S1
number: 544
sprint: s2fcksg
blocked_by:
- 01M19V3EG9GTFVFWNH5BB749K6
comments:
- id: 01M1A2NHXA0VGGCMCN3W6FH6FM
  author: Steve Vine
  at: 2026-08-30T19:34:44.906069Z
  text: |-
    Done — PR #552, merged to main.

    Leaving the page now asks too, with the same three answers COM-543 settled: the left nav, browser Back, a link, the panel on a control's own detail page. A cancelled Back leaves the address bar where it was; Discard resumes without asking twice; a failed save leaves you on the record with your edits.

    The router: main.tsx mounts createBrowserRouter + RouterProvider and App.tsx exports the same JSX tree as route elements. Route paths, nesting and elements are untouched, RootErrorBoundary stays outside the router and each shell keeps its own route boundary — neither swapped for errorElement, as the task asked.

    One guard, mounted once above the operator app, driven by useBlocker. The assessments queue drops its own provider and the page-level guard from COM-543: its exits are all route changes, so the router-level guard covers the row click, Next, Previous and close. One mechanism rather than two that could both fire.

    Sweep: the report wizard registers too — four steps written only at the end is a real loss. It saves itself and then navigates, so the hook gained a clear() for an editor to call immediately before its own navigation. The vendor form builder was left alone: its editing happens in a modal that is closed before anything can navigate.

    Tests: a new six-case suite on a two-page router (link asks, Keep editing holds the location, Discard proceeds and never saves, Save and continue saves then leaves, a failing save keeps you put, a clean editor is silent, cancelled Back leaves history intact). test-utils now mounts a memory data router plus the provider, so every test runs the shape the app runs, and PortalRouting gets a renderApp() over the real route tree.

    One jsdom shim was needed in test-setup.ts, noted in case it surprises anyone later: a data router builds a Request per navigation carrying an AbortSignal from jsdom's AbortController, and Node's Request (jsdom has none) refuses a signal from another realm. The app has no loaders and passes no signals of its own, so the shim drops the signal and keeps instanceof Request answering.
assignee: steve
label:
- improvement
priority: medium
task_status: done
---
COM-543 catches the exits inside the Assessments queue — another row, Next, Previous, close. It cannot catch leaving the page: click Controls in the left nav, or hit browser Back, or edit the same assessment on a control's own detail page and navigate off it, and the typed work still disappears without a word. This task closes the remaining route-level hole, for the assessment panel and for any other screen that holds unsaved edits.

**Expected** — the same prompt, from the same conditions, whichever way you leave: **Save and continue**, **Discard changes**, **Keep editing**. Browser Back that is cancelled leaves you where you were with the address bar unchanged.

**The constraint** — react-router only offers `useBlocker` under a data router, and `main.tsx` mounts a plain `<BrowserRouter>` around `App`'s ~80 routes. So this task is really two: convert the router, then hang the guard off it.

**Implementation**
1. Convert `main.tsx` to `createBrowserRouter` + `RouterProvider`. The route tree in `App.tsx` moves to a route-object array (or stays JSX via `createRoutesFromElements`, which is the smaller diff). Keep the provider nesting as it is — `RootErrorBoundary` stays outside the router (COM-467's last-resort catch), and each shell keeps its own route boundary; don't swap either for the router's `errorElement` in this task.
2. Five test files mount `BrowserRouter`/`MemoryRouter` directly — they keep working with `MemoryRouter`, but check any that assert on navigation.
3. Add a small "unsaved changes" hook wrapping `useBlocker` + `beforeunload`, and have the assessment panel register its dirty flag with it. Reuse whatever confirm dialog COM-543 lands rather than a second one that words it differently.
4. Sweep for other screens that hold unsaved edits and should register too — the vendor form builder and the report wizard are the likely ones; adopt them only where the loss is real.

**Sequencing** — do it after COM-543, which settles the wording and the save-then-continue behaviour. If the router conversion turns out to be a bigger diff than it looks, split step 1 into its own PR and land the guard behind it.
