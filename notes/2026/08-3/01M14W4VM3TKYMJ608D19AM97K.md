---
id: 01M14W4VM3TKYMJ608D19AM97K
created: 2026-08-28T19:04:34.179885Z
updated: 2026-08-29T12:21:26.552998Z
type: task
title: 'The report wizard: author a report without writing anything'
project: 01KXGC5PTGYHV30VM3E78G76S1
number: 491
sprint: s42ntc9
blocked_by:
- 01M14W4FVE8EZ0PHA2S8TRFC45
comments:
- id: 01M16QFDB90E7FS699XJB50NJ4
  author: Steve Vine
  at: 2026-08-29T12:21:25.993553Z
  text: |-
    Done and merged to main — PR #504.

    Four steps, and the wizard genuinely knows nothing about users, groups or devices of its own: the field list, the operators offered and the shape of the value input all come from the catalogue's declared type. A date field offers a date, a boolean offers Yes/No, an enum offers its values, and nothing is free-text that is not a text field. There is a test that switches a condition from a text field to a boolean and asserts *contains* disappears — which is the property that makes adding a subject one change rather than two.

    An operator that takes no value shows no value input: an inert box invites somebody to fill it in and wonder why it made no difference.

    **The live preview needed a new endpoint.** `POST /reports/preview` runs a definition that has not been saved, with the same validation and the same scoping as a real run. It inherits the create schema so the wizard cannot preview something it could not then save — a dead end discovered at the last step is worse than one discovered at the first. Nothing is persisted and no run is recorded: a history full of an author's twenty attempts at a condition would bury the copies somebody actually took. Refusals show in the preview while the author is still looking at the condition that caused it.

    Editing and duplicating are the same wizard seeded differently, as the task asked. The seed goes through the form's `useState` initialisers rather than an effect — the `react-hooks/set-state-in-effect` lint rule caught my first version and was right — so the loading shell is split from the form, with a `key` on the form so navigating between two reports re-seeds instead of keeping the first one's answers.

    Changing the subject clears the conditions and resets columns and sort to the new subject's defaults. They belonged to the old subject, and carrying them across would produce a definition naming fields the new one has never heard of — the refusal would then arrive at save time rather than at the change that caused it.

    One accessibility detail worth noting for the next multi-row form: the first version put a visible label on row one *and* an aria-label on every control, which gave one control two accessible names and made the tests ambiguous. Column headings now render once above the rows, and each control carries a per-row aria-label ("Condition 1 field").

    The steps do read as a sentence when done — it is the same `describe()` output the report's page and the download preamble use.
assignee: steve
company: null
label:
- feature
priority: high
task_status: done
---
ADR 0062 §2, the authoring half. Four steps, driven entirely by the catalogue (COM-487) — the wizard knows nothing about users, groups or devices of its own.

1. **What it's about** — pick the subject.
2. **Which ones** — add conditions. The field list, the operators offered, and the shape of the value input all come from the field's declared type: a date field offers *before / after / in the last N days*, a boolean offers Yes/No, an enum offers its values. Nothing free-text that isn't a text field.
3. **What to show** — pick and order the columns, pick the sort.
4. **What it's called** — name, description, the question it answers.

**A live preview of the first rows at every step**, because the honest test of a report is whether it returns what the author expected, and finding that out after saving is how people stop trusting the tool.

Editing an existing report is the same wizard, opened on its definition. Duplicating is the same wizard, opened on a copy.

The steps should read as a sentence when they're done — the summary line the report's page shows.