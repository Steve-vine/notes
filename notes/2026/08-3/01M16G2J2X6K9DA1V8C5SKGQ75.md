---
id: 01M16G2J2X6K9DA1V8C5SKGQ75
created: 2026-08-29T10:12:04.829029Z
updated: 2026-08-29T20:00:01.131645Z
type: task
title: Delete an archived company, permanently
project: 01KXGC5PTGYHV30VM3E78G76S1
number: 507
sprint: s2fcksg
blocked_by:
- 01M16G1KQ4CCD6TRMM321AYQEG
comments:
- id: 01M17HQ3KBG3QVGV48GE1T67KA
  author: Steve Vine
  at: 2026-08-29T20:00:01.131461Z
  text: |-
    Built — PR #527, CI running. Will move to review when it merges.

    The main design departure worth flagging: the graph is derived from the schema rather than listed. vendor_purge names its twelve tables and explains each, which is right at that size — but sixty-odd hand-written statements would be unreadable, and they would go stale silently. The next company-scoped table somebody adds would simply be missed, and the purge would either crash on a RESTRICT or leave rows pointing at a company that no longer exists. So core/company_purge.py seeds from every table with an FK to companies, closes over everything reachable by following FKs inward to a fixed point, and deletes in reverse dependency order. The test asserts the seed still matches the schema rather than a list in the file.

    Following keys inward is what makes it safe: a row is collected because it points at a row already condemned, so nothing shared can be swept — a global table is never the child of a company row. Both of your "must not be touched" cases fall out of that rather than needing special-casing: report_definitions with a null company never enters the seed (it selects company_id = <id>), and the directory mirror and the three settings singletons carry no company so are unreachable.

    Two things the closure genuinely cannot reach, both because they carry no FK: attachments (polymorphic owner) and notifications. The second was not in the ticket and is worth knowing — notifications point at a target by type and id, so a purged company's would have survived as reminders about work nobody can open. vendor_purge makes the same sweep for one vendor; this does it by target id across every collected row rather than a per-type map, which would go stale the next time a target type is added.

    Your two decisions:

    The activity log stays. It carries no FK precisely so the trail outlives soft-deletes and archival (ADR 0023), which means the orphaning is a decision rather than an accident. An audit trail whose job is answering "what happened to that company?" cannot be the thing that disappears when the company does — and the purge writes its own row naming who destroyed what, which would be absurd to write and then delete in the same statement.

    Evidence files go, but after the commit. purge_company returns the storage keys and the endpoint deletes the objects once the transaction has committed. A failed transaction then leaves the files intact rather than destroying bytes whose rows survived — orphaned objects are recoverable, silently missing evidence is not. A key that fails to delete is logged, not fatal.

    The four constraints that disagreed with the model are reconciled to RESTRICT in migration 0154, verified up and down against a real database. The mismatch was worse than the wrong rule: a developer reading the model believes deletion is refused, while the database would quietly take three of those tables with the company — the exact silent data loss RESTRICT exists to prevent.

    The dialog is honest about what is not cleaned up, as you asked: groups and users Compass created stay in the tenant while the record that Compass made them goes; email already sent is unaffected; the activity log is kept.

    Verification: 24 backend tests including a purge asserting no rows remain in any of the thirty scoped tables, a built-in report definition surviving, and the other company untouched. Full integration suite 1401 passed, full frontend suite 883.
assignee: steve
company: null
label:
- feature
priority: medium
task_status: active
---
A way to remove a company and its data for good, once it has been archived.

## Behaviour

- **Delete** appears only on an archived company, in Admin ▸ Companies. An active company has no delete — archive first. The default company can never be deleted.
- The confirmation states plainly that this is permanent and cannot be undone, lists what is about to go (counts of vendors, assessments, risks, decisions, access records, files), and requires the admin to **type the company's name** before the button enables.
- Admin-only.

## Why this is bigger than it looks

Company-scoped data is deliberately wired to **refuse** deletion — that is why archive exists at all (`db/mixins.py:60-71`, `models/company.py:1-6`). A real delete has to walk roughly **30 directly company-scoped tables and ~35 more reachable only through them** (~65 total), in constraint order rather than model order, because most chains are RESTRICT→RESTRICT: `companies → vendors → certifications/contacts/revisions`, and `companies → assessments → gaps → risk_gap_links ← risks`. Soft-deleted rows have to go too — `SoftDeleteMixin` entities never hard-delete today, so a purge overrides that intent.

Precedent for the shape: `core/vendor_purge.py` (12 tables for a single vendor, 6 the DB won't cascade) and the read-only pre-flight in `core/user_deletion.py`. The pre-flight is what should produce the counts shown in the confirmation.

**Four tables disagree with the model** about what happens on delete — `CompanyScopedMixin` declares RESTRICT but the migrations say otherwise: `assessment_rule_sets` (CASCADE, `0094:58`), `compliance_rules` (CASCADE, `0104:63`), `vendor_portal_branding` (CASCADE, `0100:58`), and `vendor_request_messages` (no `ondelete` at all → NO ACTION, `0086:37`). Worth reconciling as part of this rather than discovering it mid-purge.

Must **not** be touched: `report_definitions` rows with `company_id IS NULL` (built-in/global), and the tenant-wide `directory_*` mirror plus the `entra_settings` / `m365_settings` / `sso_settings` singletons.

## Two decisions to make during the work

1. **The activity log.** `activity_log` deliberately carries no FK on `company_id` so the trail outlives archival (ADR 0023; `models/activity_log.py:8-11`). A purge either leaves orphaned rows by design, or deletes the audit trail of a company that no longer exists. Take a view and record it.
2. **Files outside the database.** Evidence attachments live in object storage (`attachments.storage_key` — `risks/{id}/…`, `assessments/{id}/…`); today even a soft delete leaves the bytes. Deleting the rows without purging the objects leaves orphaned files with no pointer.

## Won't be cleaned up, and the confirmation should be honest about it
Groups and users Compass created in Entra (`tasks/access_execute.py`) stay in the tenant — while `access_changes` / `unrequested_changes`, the only record that Compass made them, go with the company. Already-sent email is gone regardless. Also guard in-flight Celery work: there is no DB outbox, so a queued `execute_access_request` could still write to Entra after the rows are gone.

## Tests
Delete refused on an active company and on the default company; refused without an exact name match; a full purge leaves no orphans across the scoped tables; global `report_definitions` and directory mirror rows survive.
