---
id: 01M0Z96370R231D1A9PTH5CP5B
created: 2026-08-26T14:56:59.616135Z
updated: 2026-08-26T19:35:56.879743Z
type: task
title: A requirement can be out of scope for a company, and the reason is on the record
project: 01KXGC5PTGYHV30VM3E78G76S1
number: 415
sprint: s8cjs5n
comments:
- id: 01M0ZS4FH5ZYFSV2TSJKVDVEG5
  author: Steve Vine
  at: 2026-08-26T19:35:43.909176Z
  text: |-
    Done — PR #421, merged to main.

    ADR 0057 is in `decisions/0057-a-requirement-can-be-out-of-scope-for-a-company.md`.

    **What shipped**

    - A `company_framework_requirements` row carries `applicable`, `justification`, actor and date — the same shape as the control assessment, for the same reason. Scope is one company's decision and the requirement library is shared reference data (ADR 0017), so two companies can disagree about the same requirement without either seeing the other's answer. There is a test for exactly that.
    - **Absence of a row means applicable**, so nothing was backfilled. Seeding 565 "applicable" rows per company would have said a decision was made where none was, and the SoA would then export a column full of decisions nobody took.
    - The justification is required when ruling out, enforced at the API rather than only the UI — the SoA is an audit artefact, and this endpoint is what a bulk scoping import will call. Ruling back in **clears** it: a reason with nothing to justify reads as a live exclusion to anyone scanning the column.
    - Excluded requirements leave the coverage denominator and the cover tally, and the excluded count is reported beside every percentage. They keep their own section in the UI with the reason visible and a way back — an exclusion is a claim somebody made, and it should be as easy to challenge as a gap.
    - The SoA's Applicable column now answers from the requirement's own decision where one exists, falling back to the old control-derived reading where none has been made. That is the exclusion justification ISO 27001 clause 6.1.3 d) has always asked for.
    - Both directions are audited by adding the table to the ADR 0023 audited set — no endpoint code, so it cannot be forgotten when the bulk-scoping endpoint lands.

    **Why this mattered more than it looks**

    COM-422, COM-426 and COM-427 deliberately add sector-specific Core controls — key custodian duties, payment-page integrity, POI tamper inspection — on the argument that per-company applicability carries them. That argument only holds at requirement level if this exists. It now does, so "Core is the superset" (ADR 0010) survives contact with frameworks that only apply to some companies.

    **One thing worth knowing**

    The requirement FK is named explicitly. The naming convention would have rendered `fk_company_framework_requirements_framework_requirement_id_framework_requirements` — 81 characters, which Postgres silently truncates to 63, leaving the metadata and the database disagreeing about what the constraint is called. COM-421 extends this table, so it was worth getting right now.

    **Tests**: `test_requirement_applicability.py` (9 cases incl. the audit trail and role gates) plus two SoA cases. Full CI green.
assignee: steve
company:
- moneypenny
label:
- feature
priority: high
task_status: review
---
ADR 0057.

Some framework requirements will never apply to a given company, and today they
read as permanent gaps that nobody can close. HIPAA's healthcare clearinghouse
isolation and group health plan documents apply to a fraction of covered
entities. PCI's payment-page script integrity means nothing to a company that
takes no cards. They will sit in the gap list forever, dragging the score down
and training people to ignore it.

ISO has the same hole from the other direction: a Statement of Applicability is
only an SoA if an excluded control carries a justification. ADR 0011 gave that to
Core controls per company. Framework requirements never got it.

## What changes for the reader

On a company's view of a framework, a requirement can be marked **not
applicable**, with a reason that is required, not optional. Out-of-scope
requirements leave the gap list and the coverage denominator, and appear in their
own section so the exclusion is visible rather than hidden.

The SoA export gains the exclusion column it has always needed.

## Implementation

- Applicability is **per company**, like the control assessment — a new
  `company_framework_requirement` row carrying `applicable`, `justification`,
  actor and date. Not a column on the shared requirement: scope is a company's
  decision, and the requirement library is shared reference data (ADR 0017).
- Default is applicable. Absence of a row means in scope — no backfill needed.
- Coverage percentages count applicable requirements only; show the excluded
  count alongside so the denominator is never silently smaller.
- Marking a requirement not applicable is an audit-trail event (ADR 0023).
  Un-marking it is too.
- The justification is free text but **not nullable when `applicable` is false** —
  enforce at the API, not just the UI.

Tests: excluding a requirement changes the denominator and the gap list; an
exclusion without a justification is rejected; two companies can disagree about
the same requirement; the SoA export renders the justification.
