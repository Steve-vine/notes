---
id: 01KXP543Q76F4SQVAAE682B51X
created: 2026-07-16T19:05:44.935151734Z
updated: 2026-07-19T13:25:11.115828371Z
type: task
title: Issue timeline feed — colour-coded bubbles
label:
- feature
priority: high
task_status: done
assignee: steve
project: 01KX671DATY39VW6GWK3M2T3DN
number: 97
blocked_by:
- 01KXP52NRFDXD3Z6KTDXGFS1G0
- 01KXP52Y2MT7KE9QTRTWGAR5PQ
- 01KXP53VCVSZB7F8AN2KSXYAX1
sprint: s0v93ii
---
The main section of the redesigned screen: render the timeline API (ISE-92) as an ordered, colour-coded bubble feed, each bubble with a date/time header, colours per the taxonomy (ISE-90).

**Reuse the assist chat primitives** (this is the payoff of building assist first): `Bubble` / `MessageBubble` / `LiveBubble` from `AssistPage.tsx:236-315`, the `useAssistTurn` reducer/state machine (`pages/assist/useAssistTurn.ts`), and the `streamTurn` SSE client (`lib/assistStream.ts`) — adapted to the issue conversation endpoint (ISE-91) so **live AI turns stream into the feed** with the tool-call trace (`LiveBubble` already renders `tool_start`/`tool_end` badges).

**Event rendering (typed union from ISE-92):**
- user / assistant message bubbles (assistant through `<Markdown>`).
- diagnosis, remediation, analyse verdict → rich bubbles rendering their **structured payloads** (root cause + confidence, remediation summary, still-present/resolved verdict), with a "view run →" link to `/agent-runs/{id}`.
- proposed-change bubbles carry a **tier badge** (T0–T3) and the exact parameters.
- approval + execution-result events (approval interaction itself is ISE-99).

Auto-scroll on new events (as `AssistPage` viewport ref does); empty / loading / error states.

Blocked by the timeline API (ISE-92), the streaming conversation (ISE-91), and the layout shell (ISE-96). Label: feature.