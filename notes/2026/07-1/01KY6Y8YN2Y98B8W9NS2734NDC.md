---
id: 01KY6Y8YN2Y98B8W9NS2734NDC
created: 2026-07-23T07:33:08.898874Z
updated: 2026-07-23T11:03:34.994498Z
type: task
title: Split panes + per-pane status bar
imported_from: linear
assignee: steve
task_status: done
comments:
- id: 01KY6Y97VZ13R9VTQ5939926BB
  author: Steve Vine
  at: 2026-07-23T07:33:18.335232Z
  text: |-
    Steve Vine · 2026-06-20:

    **Done building — in review.**

    **What was done** (frontend-only):
    - **`paneTree.ts`** — pure immutable tree helpers: `splitLeaf`/`closeLeaf` (collapse to sibling; never remove the last leaf)/`setLeafNote`/`setRatio` + `firstLeafId`/`hasLeaf`.
    - **`Pane.svelte`** — recursive: leaf → `NotePane`; split → two child Panes with a pointer-drag divider (flex ratio, clamped 0.1–0.9), row or col.
    - **`NotePane.svelte`** — the viewer/editor extracted per-pane: own note load + read/edit state + a status bar (extensible host) with **Split ⬌ / Split ⬍ / Close** + read/edit toggle.
    - **`Main.svelte`** — sidebar unchanged; right panel is the pane tree; sidebar clicks open into the **focused** pane (outlined).

    **Decision:** hand-rolled (no new dep, ADR 0002). Functional-first; visually identical to a library. Polish (keyboard resize, ARIA, hit-area, double-click reset, min-size, layout persistence, a vitest harness for the tree helpers) → filed **DEV-527**.

    **Verification** — `npm run check` (0/0), fmt, clippy `-D warnings` (exit 0), `cargo test` (41, Rust unchanged), `npm run tauri build`. **Interactive split/resize/close needs your manual sign-off** (restart `tauri:dev`).

    **Please verify:** open a note → **Split ⬌** and **⬍** repeatedly → each pane independent (different notes, edit independently) → drag dividers to resize → **Close** back down. Active pane outlined; sidebar opens into it.

    **PR:** https://github.com/Steve-vine/notula/pull/20 · commit `7d345d2`
    Holding the merge for your sign-off.
priority: medium
project: 01KY6W9951TW0904DT0GGJVGE7
number: 21
sprint: s6s57kv
---
The recursively-splittable main panel (`brief/ui.md`), iTerm2/Ghostty-style.

**Checklist**

- [ ] Split the main panel **horizontally or vertically, repeatedly**; each split is an independent pane showing its own note
- [ ] Per-pane **top status bar** built as an extensible host for controls, with at least the **read/edit** toggle (from DEV-515) living there
- [ ] Resizable splits; a way to close a pane
- [ ] Each pane tracks its own note + read/edit state independently

**Done when:** the main panel can be split H/V repeatedly, each pane shows its own note with its own status bar + read/edit toggle.

**Scope:** layout persistence across restarts is optional/later. Depends on DEV-515 (the pane content = viewer/editor).

---

Linear DEV-516 · M4 — Main UI · created 2026-06-20 · done 2026-06-20