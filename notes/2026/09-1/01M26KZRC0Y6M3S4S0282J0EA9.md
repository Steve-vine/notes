---
id: 01M26KZRC0Y6M3S4S0282J0EA9
created: 2026-09-10T21:36:09.088491Z
updated: 2026-09-11T19:27:54.834245Z
type: task
title: Inventory links — risks, decisions, vendors and controls on both registers, shown from both ends
project: 01KXGC5PTGYHV30VM3E78G76S1
number: 667
sprint: skdc1az
blocked_by:
- 01M26KZ3K106KB0HJSKK03TJCQ
- 01M26KZF0BGCPFD74X2GSAVVS0
comments:
- id: 01M26SRA2HXD6GJ518P3BPT2KF
  author: Steve Vine
  at: 2026-09-10T23:16:56.529207Z
  text: |-
    Done — PR #675 merged to main.

    Backend: `inventory_risk_links` and `inventory_control_links` (asset side polymorphic over the two registers, other side a RESTRICT FK), `data_asset_recipients` (vendor + optional DPA reference), `containers.supplier_vendor_id`, and `decision_link_target_type` gains container/data_asset (migration 0180). Routes: GET/POST/DELETE …/risks and …/controls on both registers; supplier on the container body; recipients full-set on the data asset body; reverse reads GET /risks/{id}/assets, /controls/{ref}/assets?company=, /vendors/{id}/assets. Every link counts as a citation in the guarded delete. Decision links now resolve to a label and a path for every target type. 4 integration tests covering the acceptance path.

    Frontend: Risks (with score pills), Protecting controls and Decisions cards on both asset detail pages using the shared two-step picker; supplier shown on Hosting and picked in the modal; recipients shown under Personal data and edited in the modal; risk detail shows Assets, control detail Protected assets (selected company), vendor detail Assets (supplies / receives data), decision detail "Applies to" with resolved links.
assignee: steve
label:
- feature
priority: medium
task_status: done
---
An asset is only useful to governance if the rest of Compass can cite it (ADR 0072). Links between the two registers and the existing entities, visible from either end.

* **Risks** ↔ container / data asset — the `risk_control_link` shape; a risk's detail lists the assets it concerns, an asset's detail lists its risks with score pills.
* **Decisions** ↔ container / data asset — the `decision_link` polymorphic shape gains the two target types; the decision detail's linked-entities section shows them.
* **Vendors**: a container's **supplier** (single vendor FK, for SaaS and third-party-dependent assets, linking to the vendor's risk tier and assessments); a data asset's **third-party recipients** (many vendors, each with an optional DPA reference). The vendor detail gains an **Assets** section listing both.
* **Controls** ↔ container / data asset — Core controls that protect the asset; the control detail lists assets in the selected company.
* Each link is a join table in `_AUDITED_TABLES`; the link editors reuse the existing picker components (the risk↔control picker) rather than a new one.
* Global search wiring for the two registers is in the finishing task, COM-673; this task adds the link surfaces only.

**Acceptance**: link a risk, a decision, a vendor and a control to a container and to a data asset; each appears on the other record; unlinking is audited; a vendor with linked assets shows them.