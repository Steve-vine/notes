---
id: 01M32YZH7M51X6EJ09F9AMS175
created: 2026-09-21T21:47:00.2128Z
updated: 2026-09-22T20:48:29.91857Z
type: task
title: Write the install guide
project: 01M32Q7WT6058ZQFMMRM8KP1K6
number: 2
comments:
- id: 01M35D3FEY6NH67DSDVG9QZAY4
  author: Steve Vine
  at: 2026-09-22T20:32:18.39865Z
  text: |-
    Pushed to staging. git pull only - no npm install needed.

    What to look at: /docs/install/. It runs: what you need - try it on a laptop cluster - install for real (the Secret, attachments, the public address, then install, the first administrator) - reaching it and HTTPS - after the first sign-in (Attachments, Email, Integrations for Microsoft sign-in) - the Vendor Portal - upgrade and uninstall - reference.

    Version number: the page says "The current release is 0.7.0" and every helm command carries 0.7.0. That number is read from the public chart registry each time the site is built, so a new release appears on the next build with no edit. If the registry cannot be reached the build uses a fallback (0.7.0 today) and says so in the log.

    Checked: every command and value name against chart/README.md, values.yaml and templates/NOTES.txt at v0.7.0; the admin screen names (Attachments, Email, Integrations) and the Microsoft sign-in fields against the app's admin screens; the create-admin command against the CLI. The chart did not change between 0.6.0 and 0.7.0.

    Not checked: the guide has not been followed end to end on a fresh cluster. That is the "done when" in the task, so a run-through on minikube or kind before launch would be worth doing.

    Technical: src/content/docs/docs/install.mdx (MDX, because the version is templated in through Starlight's <Code> component); src/lib/release.ts does the lookup. Recorded in CLAUDE.md.
assignee: steve
label:
- feature
priority: high
task_status: done
---
"Install Compass" is the main button on the home page and currently leads to an empty page. The app repo is private, so visitors cannot read the chart's README - the website has to carry the install guide and the current version number itself.

Cover: what you need first (Kubernetes 1.28+, PostgreSQL 16+, somewhere for attachments), a quick evaluation install, a production install, creating the first admin, setting the public URL and HTTPS, choosing the attachment store, and optional Microsoft Entra ID sign-in.

Done when: someone with a cluster can go from nothing to a signed-in admin using only this page, and every command has been checked against the chart.

Technical: source is chart/README.md and chart/values.yaml in the app repo. Install reference is oci://ghcr.io/steve-vine/compass/charts/compass --version x.y.z (public). Page: src/content/docs/docs/install.md. Decide how the current version number gets onto the page without hand-editing each release.