---
id: 01M2WG0N2W05CB50CAPRF222Y3
created: 2026-09-19T09:30:01.692591Z
updated: 2026-09-24T20:29:37.929439Z
type: task
title: Production's values match what was actually built — bucket, role, public address, and whether the internal ingress stays
project: 01KXGC5PTGYHV30VM3E78G76S1
number: 720
sprint: stek6vx
comments:
- id: 01M2WM5FSA0Y4RFY1ESAJXQKFQ
  author: Steve Vine
  at: 2026-09-19T10:42:34.410164Z
  text: '2026-09-19 progress. Bucket and role names: corrected by Steve in both values files (devops f9185de). Database backups: WAL archiving was NOT working — the `standard` CNPG image has no barman-cloud (COM-727); fixed with the `system` image (devops 8c8fae5) and verified from the bucket, 23 WAL objects from segment 1 onward at 10:42 UTC. Still to confirm: the first **base** backup — the 02:00 ScheduledBackup tonight, check `kubectl -n compass-prod get backup` tomorrow or look for `postgres-backups/compass-postgres/base/` in the bucket. Still open: `config.appBaseUrl` vs the Cloudflare Tunnel hostname; whether the internal ingress and certificate stay.'
- id: 01M2YXGGVGPSTB9Z6664ZTXP1W
  author: Steve Vine
  at: 2026-09-20T08:04:22.000742Z
  text: |-
    2026-09-20 08:05 UTC — first base backup confirmed from the bucket: `base/20260920T020000/` (backup.info `status=DONE`, `error=None`, 02:00:00→02:00:09 UTC, data.tar.gz 17 MB). WAL archive: 305 segments, numbered 1→305 with no gaps, one every ≤5 min (archive_timeout) right up to 08:00. So point-in-time recovery is possible from 2026-09-20 02:00 UTC onward. Note the ScheduledBackup cron is UTC: "2AM" is 03:00 BST.

    Not yet proven: a restore. Worth a task — restore the backup into a scratch cluster once and time it.

    Observation: the bucket has only `postgres-backups/` — no attachment objects yet, i.e. nobody has uploaded a file in production (or Save was never pressed on Admin ▸ Attachments and the first upload will be the test). Upload one evidence file to confirm the S3 path end to end.

    Still open on this task: `config.appBaseUrl` vs how staff actually reach the app (Twingate → compass.moneypenny.uk: matches); whether the internal ingress and certificate stay (they are now THE employee route, so yes — this item can close).
- id: 01M2YXVQRG8GF580Q300VBWCZ3
  author: Steve Vine
  at: 2026-09-20T08:10:29.51982Z
  text: '2026-09-20 overnight check, from Steve''s output: every pod 0 restarts (worker 15h on the 2Gi limit, postgres 21h); last 12h of worker tasks all `succeeded`, none raised — heartbeat/mail health/Entra health 144 each, directory sync 48, report schedules 48, sign-in sweep / MFA sweep / reminders 12 each, posture snapshot 1, lifecycle 1, recert trigger 1. Counts match the schedules exactly, so Beat missed no tick. (The 5-minute access-execution sweep runs in the `worker-execution` container and is not in this log.) Restore test raised as COM-730.'
assignee: steve
label:
- follow_up
priority: high
task_status: done
tech: null
---
A check-and-correct task on `Moneypenny-Development/devops.application.compass`, from the 2026-09-19 production install. The values files were written before the AWS resources existed and guessed their names.

**Check, and fix where still wrong**
- **Bucket and role.** What exists: bucket `mp-envproductionpri-compass-prod-files`, role `compass-app`. The files as first committed say `mp-envproductionpri-compass-attachments` and `compass-prod-app` — in `envs/prod-uk-compass-app.yaml` (`config.s3.bucket`, both ServiceAccount annotations) and `envs/prod-uk-compass-env.yaml` (`postgres.serviceAccountRoleArn`, `postgres.backup.destinationPath`). If Admin ▸ Attachments tests green the app half is already right; the **database backup path** is the half nothing on a screen would reveal — confirm `kubectl -n compass-prod get cluster compass-postgres` reports continuous archiving working and a first `Backup` completed.
- **The public address.** Production is reached through a Cloudflare Tunnel. `config.appBaseUrl` must be that hostname or emailed links (password reset, notifications) point somewhere else.
- **The internal ingress and certificate** for `compass.moneypenny.uk` in the env app: keep if both routes are wanted, remove from `envs/prod-uk-compass-env.yaml` if the tunnel is the only way in.
- `readme.md` bootstrap commands in that repo name the guessed bucket and role — align them.

**Acceptance**: every name in the two values files and the readme is one that exists; a nightly database backup has landed in the bucket; a password-reset email links to the address people actually use.