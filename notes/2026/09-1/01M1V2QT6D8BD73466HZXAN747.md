---
id: 01M1V2QT6D8BD73466HZXAN747
created: 2026-09-06T10:03:04.269255Z
updated: 2026-09-06T13:44:02.837422Z
type: task
title: a risk is written as cause, event and consequence
project: 01KXGC5PTGYHV30VM3E78G76S1
number: 581
sprint: s2fcksg
comments:
- id: 01M1V2VX83YFP4M4SNT340A469
  author: Steve Vine
  at: 2026-09-06T10:05:18.4674Z
  text: 'Steve confirmed, 2026-09-06: all three fields optional, and none of them blocks a save. The form should still make a cause-only risk read as unfinished, but nothing is enforced at the API.'
- id: 01M1VFCDNCZMMM3G2QG49J3B2P
  author: Steve Vine
  at: 2026-09-06T13:44:02.476786Z
  text: |-
    Done — PR #592, merged to main.

    `description` is now **cause**, **event**, **consequence**, plus **notes**. All four optional, none blocking a save, exactly as confirmed — the form makes a cause-only risk read as unfinished and the API enforces nothing. Notes is on the detail page only, absent from the New risk dialog for the reason in the task.

    The scores moved as specified: the pairing flips from *Inherent (likelihood, impact) / Residual (likelihood, impact)* to **likelihood (inherent, residual)** beside the event and **impact (inherent, residual)** beside the consequence. The derived score-and-band badges move to their own row, one per pair, since with the scores split across two rows neither badge belonged to either row alone. The New risk dialog is `size="xl"`.

    **On the migration — the one thing worth reading.** It *moves* the description into `cause` rather than dropping it, with the other two left empty. Staging holds one risk with no description, so that branch is a no-op there; I wrote it anyway because production is not staging, and because CI only ever migrates an empty database — the transformation branch would otherwise ship completely untested. Verified locally against a populated database: a risk with a description gets it in `cause`, a risk without one is untouched, revisions keep their own `description` un-copied, and the downgrade round-trips.

    So the "before production, check that again" caveat is discharged: whatever descriptions exist by then will be placed, not lost.

    Risk revisions keep their `description` column, and nothing sets it any more. History reads as it was written.

    Also: global search indexes all four new fields; the result snippet reads the event, falling back through cause and consequence since all three are optional. **The risk register report never carried `description`**, so nothing was silently dropped there — I left it alone rather than adding three prose columns to a fixed-width landscape table. Say if you want them.

    Two incidental notes: the migration is `0166_risk_statement_three_parts` because `alembic_version.version_num` is `varchar(32)` and my first name for it was 33 characters. And GitHub silently dropped the CI event for this branch three times (dispatch, reopen, amend-and-force-push all produced zero check-runs while sibling PRs triggered normally) — a rebase onto current main fixed it.
assignee: steve
label:
- feature
priority: high
task_status: review
---
Raised by Steve, 2026-09-06, to bring risk statements into line with how the ISO standards ask them to be written.

A risk is currently a **title** and a free-text **description**, which lets somebody write "Ransomware" and stop. What a risk statement is supposed to say is: *because of this condition, this event might happen, and if it does these objectives suffer.* One box invites the first third of that.

## What changes

**`description` is replaced by three fields:**

- **Cause/condition**
- **Potential event**
- **Consequence/impact on objectives**

All three are **optional** and none blocks a save (Steve, 2026-09-06) — a half-written risk is still worth recording. The form should still make a cause-only risk read as unfinished; nothing is enforced at the API.

**The scores move next to the text they score** (Steve's call — both pairs):

```
Cause/condition
[                                        ]

Potential event                 Likelihood
[                          ]   inherent [3] residual [2]

Consequence/impact              Impact
[                          ]   inherent [4] residual [4]
```

That layout is the argument for itself: **likelihood is the chance of the event, and impact is the severity of the consequence.** Putting each score beside the sentence it grades is what stops the numbers being chosen in the abstract — the same problem COM-579 addresses from the other side by naming the levels. The two together turn scoring from "pick a 3" into "how likely is *this event*, on this scale".

## A Notes field

Added by Steve, 2026-09-06: a free-form **Notes** box in the risk's **Details & scoring** section, at the bottom.

**Deliberately not on the New risk dialog.** Raising a risk is about stating it; notes are what accumulate while it is being worked. Keeping them off the creation form is what stops the three structured fields being bypassed by somebody dumping everything into a general box — which is the habit this whole task exists to break.

It is another column on the same migration, so it rides along here rather than arriving separately. It joins the revision snapshot like every other field on the record, and should be searchable alongside the three new ones.

## The New risk dialog has to fit

Three text boxes and four score pickers do not go in a default-width dialog — the same complaint as COM-563 and COM-578, and this task would create a third instance of it if the size is left alone. Size it to the content it now holds.

*(Three separate tasks have now had to widen a default dialog. Worth asking, separately, whether the default is simply wrong for anything that authors a record.)*

## Now is the cheapest possible moment

Staging holds **one risk, with no description at all**, and two revisions. So there is nothing to preserve and no transformation to get right. Do this before real risks are written and it is a schema change with no data problem attached.

**Before production, check that again.** If any risk has a description by then it has to be placed, not dropped — the honest destination is Cause/condition (it is where an unstructured statement usually starts) with the other two left empty, rather than a guess at splitting a sentence three ways.

## What else it touches

- **Risk revisions** snapshot the description. History is immutable, so old revisions keep the field they were written with; the new fields join the snapshot from here on. Do not rewrite history to make it look as though risks were always written this way.
- **Global search** indexes title and description today. It should index the three new fields and the notes — otherwise searching for a consequence you know you wrote finds nothing.
- **The API changes shape** (ADR 0004): schemas, the snapshot field list, regenerate `schema.d.ts`. Check the risk PDF and CSV reports render the new fields rather than silently dropping the removed one.

## Related

- COM-579 — naming the levels on the likelihood and impact pickers. Same screens; land them together and scoring reads properly end to end.
- COM-563 / COM-578 — the other two dialogs too small for what they hold.
