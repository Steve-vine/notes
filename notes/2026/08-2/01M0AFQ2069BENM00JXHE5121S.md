---
id: 01M0AFQ2069BENM00JXHE5121S
created: 2026-08-18T13:07:03.814829Z
updated: 2026-09-01T13:55:51.400147Z
type: task
title: Group→role mappings — role policy defined in Compass, assignment in Entra
project: 01KXGC5PTGYHV30VM3E78G76S1
number: 250
sprint: s5thbzy
blocked_by:
- 01M0AFPMPJQWC1PQ7ME7G5CQ4N
comments:
- id: 01M0AKQHDTK0R5PSGMWK83EF4D
  author: Steve Vine
  at: 2026-08-18T14:17:13.914054Z
  text: |-
    Merged to main (PR #248, full suite green). What shipped:

    - **Model**: `sso_group_role_mappings` (Entra group object id unique + display name captured at creation) with role rows on the `user_roles` pattern; migration 0069 reuses the `user_role` enum via `postgresql.ENUM(create_type=False)`. The mapping table is audited; role-row edits audit via a touch on the mapping row (composite-PK caveat).
    - **Resolution** (`core/role_resolution.py`): effective roles for an Entra user = union of mapped roles across their SCIM-synced groups, **recomputed and persisted** on: SCIM membership change (the COM-249 hook is now live), mapping create/update/delete, and **defensively at SSO sign-in**. Unmapped users hold no roles — no silent viewer default. SCIM user create/adoption now derives too, so an adopted local account's manual roles drop (derived-only, per the ADR).
    - **Derived-only enforcement**: `PATCH /users/{id}` refuses role edits for Entra accounts with a 409 pointing at the mappings; local accounts unchanged.
    - **Admin API**: `/api/v1/sso/group-role-mappings` CRUD — list carries `affected_user_count` (blast radius) and `group_synced` (warning state); display name auto-fills from the synced group, free-entry before first sync. Plus `GET .../user-sources` for the "via group" provenance COM-251 renders.
    - **Guard rail**: newly granting `admin` or an Access role returns `confirmation_required` naming the roles + affected-user count until `confirm_privileged: true` — the ADR-settled alternative to maker-checker.
    - 7 integration tests incl. end-to-end membership→roles through `/scim/v2` and a stale-privilege drift corrected by the sign-in defensive refresh through the real OIDC stub.
assignee: steve
label:
- feature
priority: medium
task_status: done
---
The sprint's role-model decision made concrete: Entra group membership drives Compass roles, but the mapping is **Compass-owned and audited** — e.g. `compass-vendor` → vendor_manager + vendor_owner. Role *policy* changes stay in Compass's activity log; day-to-day *assignment* happens in Entra.

* **Model**: `sso_group_role_mappings` — Entra group (object id + display name captured at creation), → one-or-many Compass `Role` values; unique per group; joins `_AUDITED_TABLES` (a mapping edit is a privilege-policy change — exactly what an auditor asks about). Global, not company-scoped (app roles are global, ADR 0026).
* **Resolution**: effective roles for an `auth_provider="entra"` user = **union of mapped roles across their SCIM-synced groups**. Users whose groups map to nothing hold no roles and surface visibly as "no mapped roles" (decided in the ADR — no silent viewer default). Manual role editing is disabled for Entra users (derived-only; the Users section explains why and links the mappings panel); local accounts keep manual roles.
* **Recompute triggers**: SCIM group-membership PATCH (primary), plus a defensive refresh at SSO sign-in so a missed webhook can't leave stale privilege. Role changes recompute-and-persist (not computed per-request) so the Users list, activity log and recert-style reviews all see concrete values.
* **Admin UI**: a Mappings panel (Admin ▸ Users or ▸ Integrations — decide with the frontend task): group picker fed from the synced SCIM groups (or free-entry object id before first sync), multi-select of Compass roles, affected-user count per mapping, warning state when a mapped group no longer syncs.
* **Guard rail**: mappings that grant `admin` (or the ADR 0045 access roles) deserve friction — at minimum a confirmation naming the blast radius; consider requiring a second admin (the maker-checker instinct applied to privilege policy). Decide in the ADR, implement here.

Refs: ADR 0046, 0026, 0023, 0045 (role-vocabulary discipline); COM-249 (SCIM groups feed).