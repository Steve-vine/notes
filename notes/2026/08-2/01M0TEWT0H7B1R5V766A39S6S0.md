---
id: 01M0TEWT0H7B1R5V766A39S6S0
created: 2026-08-24T18:00:34.577315Z
updated: 2026-08-24T22:04:31.306842Z
type: task
title: Gantt view task background
project: 01KY6W9951TW0904DT0GGJVGE7
number: 401
sprint: segj1dz
comments:
- id: 01M0TJCN15ZH67F4JGWXMRNZE4
  author: Steve Vine
  at: 2026-08-24T19:01:39.491798Z
  text: |-
    Done — PR #390 (branch brief-400-401-substitutions-and-gantt-rail), landed alongside NOT-400.

    Cause: .rail is sticky over the track and paints itself opaque (var(--pane-bg)) so labels ride over the bars when the chart scrolls. .row.selected .rail replaced that with rgba(var(--accent-rgb), 0.15) — translucent — so the bars showed through the task title.

    Fix: background: color-mix(in srgb, var(--accent) 15%, var(--pane-bg)) — same colour, opaque. Matches the idiom .row.band already uses.

    Left alone deliberately: .unsched-list .open.sel keeps its translucent tint. It sits in the unscheduled panel, not over the track, so there's nothing behind it to bleed through.

    Needs an eyeball: select a task, then scroll the chart horizontally and confirm the title stays readable.
- id: 01M0TWVFPA26X8YCZN2KTJ8TPZ
  author: Steve Vine
  at: 2026-08-24T22:04:31.306451Z
  text: 'Merged — PR #390 squashed onto main as b2b793e, alongside NOT-400. Done.'
assignee: steve
priority: medium
task_status: done
---
In Gantt view when a Task timeline is selected, the task title background becomes transparent, that shouldn’t happen.

## Cause

`.rail` (the label column) is `position: sticky; left: 0` with an opaque
`background: var(--pane-bg)`, so labels ride over the bars when the chart is
scrolled horizontally. `.row.selected .rail` then replaces that background with
`rgba(var(--accent-rgb), 0.15)` — a *translucent* tint — so the bars underneath
show through the title.

## Agreed work

Paint the selection tint as an opaque colour: mix the accent into `--pane-bg`
rather than laying a translucent accent over nothing. Same for the project row
and the unscheduled list's selected entry, which use the same pattern.