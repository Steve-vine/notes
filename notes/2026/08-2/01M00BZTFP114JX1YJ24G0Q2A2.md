---
id: 01M00BZTFP114JX1YJ24G0Q2A2
created: 2026-08-14T14:49:32.406937Z
updated: 2026-08-14T19:01:55.249024Z
type: task
title: the cloud says how much of the pool it is showing
project: 01KX671DATY39VW6GWK3M2T3DN
number: 720
sprint: svc641e
comments:
- id: 01M00TDSV6NVKVE6953QPCEDMJ
  author: Steve Vine
  at: 2026-08-14T19:01:50.566557Z
  text: |-
    Built and merged — PR #669, released to main 2026-08-14. Last of the five.

    `total` comes back from the cloud endpoint via `COUNT(*) OVER ()`, which Postgres evaluates ahead of the `LIMIT` — so it is the pool, not the page, in the same single statement. No extra query, and the perf test is unchanged.

    Two decisions worth recording:

    1. **The total is scoped to the same filters as the page.** It counts under the current window, integration filter AND search. The alternative — always reporting the whole estate's 2,279 — would make the footer contradict itself after a search: "1 of 2,279" reads as a broken screen, not as a narrow result. It has to answer "of what?" the same way the page does.
    2. **The badge lost its number and gained the rule.** Once the true total sits beside it, "Showing the 200 hottest tags" is redundant on the count and silent on the remedy. It now reads "Showing the hottest tags — search or re-sort to reach the rest", switching to "most-carried" under an entity sort. That is the sentence that makes ISE-717 and ISE-718 legible as the way to the rest of the pool rather than as decoration — which was this task's stated reason for existing.

    The footer thousands-separates (`3 of 2,279 tags`). Tested with a stub returning `total: 2279` against 3 items, and the untruncated case asserts no badge renders at all.
assignee: steve
label:
- improvement
priority: medium
task_status: review
tech: null
---
The cloud renders at most `CLOUD_LIMIT = 200` tags. On staging the pool holds **2,279** — so 91% of it never reaches the browser, and the screen does not say so in any way an operator would notice.

What it says today (`app/frontend/src/pages/TagCloudPage.tsx:199-208`):

- `200 of 200 tags` — reads as "that's all of them"
- a small yellow badge: `Showing the 200 hottest tags`

The badge is honest about the cap but silent about the scale of what was dropped, and the count line beside it actively contradicts it. The existing code comment says "a silently truncated cloud reads as the whole estate when it is not" — the intent is right, the execution understates it by an order of magnitude.

Return the pool total from the cloud endpoint (count before `LIMIT`, under the same window/system/search filters) and say `200 of 2,279 tags`. The badge can then explain the *rule* rather than the number — that the cloud keeps the hottest, and the rest are reachable via search and sort.

This is the smallest of the four cloud fixes and the one that makes the other three legible: once an operator can see that most of the pool is hidden, "sort by entity count" and "search" read as the way to reach it rather than as decoration.

**Done when:** the footer states the true pool size, and the truncation notice explains how to reach what is not shown.