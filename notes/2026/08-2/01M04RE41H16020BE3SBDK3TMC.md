---
id: 01M04RE41H16020BE3SBDK3TMC
created: 2026-08-16T07:44:01.585036Z
updated: 2026-08-25T18:43:00.534087Z
type: task
title: 'Portal register: vendor rows highlighted, engagement sub-rows in page colour'
project: 01KXGC5PTGYHV30VM3E78G76S1
number: 217
sprint: sbph5q5
blocked_by:
- 01M02Y5ZQ9JM55V3YJH27XDZDZ
comments:
- id: 01M05085P4NW6HG0ZTK8K7YBVV
  author: Steve Vine
  at: 2026-08-16T10:00:35.268557Z
  text: |-
    Done — PR #215, merged to main.

    Dropped `striped` (the app theme sets `striped: 'odd'` as a Table default, so it had to be an explicit `striped={false}`) and coloured by row meaning instead: vendor rows carry `data-group-row="parent"`, engagement sub-rows `"child"`, with two rules in `index.css` giving parents `var(--table-striped-color)` and children a transparent background.

    Theme tokens only, as asked — `--table-striped-color` is set on the table root in both colour schemes whether or not striping is on, so light and dark are correct by construction with no hard-coded colour anywhere. `highlightOnHover` still wins because Mantine's hover rule is a class plus a pseudo-class, which outranks an attribute selector.

    Deliberately wrote the CSS rules generically rather than scoping them to the portal register, so COM-223's grouped Requests list can adopt the same scheme by adding the same attributes.

    Tests: the parent/child marking, and that no row is striped by index.
assignee: steve
company: null
label:
- improvement
priority: medium
task_status: done
---
Follow-up to COM-212 from smoke-testing: the register table still uses Mantine `striped`, which alternates shades by row index — so with engagement sub-rows in the table, the striping cuts across vendor/engagement grouping arbitrarily.

- [ ] Drop `striped` on the `PortalVendorsPage` table; colour by row *meaning* instead: **vendor rows get the highlight shade** (the colour today's first striped row has), **engagement sub-rows get the plain page background** — so each vendor group reads as a highlighted header with its engagements beneath.
- [ ] Use theme tokens (the striped background var), not hard-coded colours — must hold up in light and dark mode.
- [ ] Keep `highlightOnHover` behaviour sensible with the new scheme.
- [ ] Tests/snapshot updated.

Frontend only; stacks on COM-212.