---
id: 01M0Q67QE392HEGF8CPFXV4771
created: 2026-08-23T11:31:31.907954Z
updated: 2026-08-23T13:30:53.758709Z
type: task
title: Device mirror — the sync learns devices and their group memberships
project: 01KXGC5PTGYHV30VM3E78G76S1
number: 386
sprint: s5gwx0s
comments:
- id: 01M0QD29DY8GKR2W3FWZEHH429
  author: Steve Vine
  at: 2026-08-23T13:30:53.758581Z
  text: |-
    Done — PR #388 (feature/com-386-device-mirror).

    The defect this fixes, restated: the $batch edge crawl fetched every group's member ids and dropped the device ones, so a device-only security group read as empty. The crawl now classifies into a third bucket instead of the bin.

    What landed:
    - `directory_devices` — the `directory_users` idiom over a different object kind (Entra object id PK, `vanished_at` marking never deletion, not audited). Display name, deviceId, OS + version, enabled, managed, compliant, trust type, approximate last sign-in, registration.
    - `directory_group_device_members` + `directory_device_owners`, reconciled every pass like user membership/ownership. Deliberately their own tables rather than a member-kind column on `directory_group_members`: everything downstream of that table is the governable surface, which stays user-only by construction instead of by remembering a filter.
    - Full pass: one extra paged `/devices` read with `$expand=registeredOwners($select=id)`, with a `$batch` owner-crawl fallback for a tenant that 400s on the expansion (tested). Delta pass: `/devices/delta` + stored `devices_delta_link`.
    - `DirectorySyncStatus` grows `devices_count` + `devices_delta_link`; the Integrations health card shows the device count.
    - No new consent — `Directory.Read.All` covers `/devices`.

    Baseline guard: devices raise no unrequested-change items at all, so the first pass that sees them cannot flood the validation queue. `test_device_membership_raises_no_unrequested_change` asserts both halves — a device joining a managed group raises nothing, a *user* joining the same group still does — so the exclusion reads as deliberate rather than as a broken detector.

    Two things worth knowing beyond the brief:
    - `trustType` outside Entra's documented vocabulary mirrors as `unknown` rather than dropping the device; the inventory says "unknown", never nothing.
    - The ledger's `memberships_count` now includes device memberships — it reports what the crawl reconciled, and a device-only group is no longer silently zero.

    ADR 0045 amended (2026-08-23) with the widened mirror scope.

    Migration 0105_device_mirror; applies and round-trips down/up. Full backend suite green (201 unit + 739 integration); schema.d.ts regenerated.
assignee: steve
label:
- feature
priority: medium
task_status: active
---
Foundation for read-only device support in Access Control. **Headless by design** — no user-facing surface of its own; the screens land in the dependent tasks. The point: the `$batch` edge crawl already fetches every group's member ids and *drops* the ones that are devices (`tasks/directory_sync.py` — "devices, service principals are dropped"), which is why device-only security groups read as empty.

- [ ] `directory_devices` mirror table, same idiom as `directory_users` (Entra object id `String(64)` PK, `vanished_at` marking never deletion, not audited): display name, `device_id`, OS + version, `account_enabled`, `is_managed`, `is_compliant`, `trust_type`, approximate last sign-in, registration datetime.
- [ ] `directory_group_device_members` edge — the crawl classifies member ids into a third bucket instead of the bin. Current-state fact, reconciled every pass like user memberships.
- [ ] Registered owners edge (`directory_device_owners`, device → mirrored user) — cheapest via `$expand=registeredOwners($select=id)` on the devices crawl; fall back to the `$batch` pattern if `$expand` misbehaves.
- [ ] Full pass: one extra paged `/devices` read (`$top=999`). Delta pass: `/devices/delta` + a stored `devices_delta_link`; membership-only changes already surface the group via the existing groups delta `$expand=members($select=id)`, so the re-crawl mechanism needs nothing new.
- [ ] `DirectorySyncStatus` grows `devices_count` + `devices_delta_link`; the Integrations health card shows the device count alongside users/groups/memberships.
- [ ] **Baseline guard**: the first pass that sees devices must not flood the validation queue — device pairs stay entirely outside out-of-band detection for now (managed-group pair detection remains user-only; the `pre_populated` idiom is the shape if that ever changes).
- [ ] Devices are **browse-only inventory** — outside the governable surface: matrix, JML, recert and unrequested-change detection untouched. Short amendment note to ADR 0045 recording the widened mirror scope.
- [ ] No new consent: `Directory.Read.All` (already granted, `core/graph.py`) covers device reads.

Refs: ADR 0045 §3, COM-237 (mirror), COM-316 (delta), COM-315 ($batch crawl), COM-252 (scope-widening precedent).